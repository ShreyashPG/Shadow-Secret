generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = "mongodb+srv://shreyasghanekar35:Datta%4023Nagar@cluster0.8tbumzm.mongodb.net/secrets?retryWrites=true&w=majority"

}

model Secret {
  id               String   @id @default(auto()) @map("_id") @db.ObjectId
  encryptedMessage String
  isProtected      Boolean
  isRead           Boolean  @default(false)
  age              Int      @default(15)
  createdAt        DateTime @default(now())
}
